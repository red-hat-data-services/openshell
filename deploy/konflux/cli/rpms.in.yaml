# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# RPM dependencies for Dockerfile.konflux.cli
# Build + runtime stages combined.
#
# Resolved with `context.bare: true` so the lockfile pins the full dependency
# closure. The image installs RPMs into two different bases (the ubi:9.8 build
# stage and the ubi-minimal runtime stage); bare guarantees every package is
# pinned and prefetched regardless of which base already provides it.
#
# Target arches and repos are declared in this file (not passed on the command
# line) so Konflux MintMaker can regenerate the lockfile with no CLI flags.
# Regenerate manually with:
#   rpm-lockfile-prototype --outfile deploy/konflux/cli/rpms.lock.yaml \
#       deploy/konflux/cli/rpms.in.yaml
#
# UBI content is public, so repos are inlined here rather than read from the
# base image; rpm-lockfile-prototype resolves versions from the CDN and cannot
# read /etc/yum.repos.d from inside the base image.
contentOrigin:
  repos:
    - repoid: ubi-9-baseos-rpms
      baseurl: https://cdn-ubi.redhat.com/content/public/ubi/dist/ubi9/9/$basearch/baseos/os/
    - repoid: ubi-9-appstream-rpms
      baseurl: https://cdn-ubi.redhat.com/content/public/ubi/dist/ubi9/9/$basearch/appstream/os/
packages:
  # Build stage: build tools
  - gcc
  - make
  - tar
  - xz
  # Build stage: Rust compilation (rustup provides rust/cargo)
  - gcc-c++
  - cmake
  - openssl-devel
  - git-core
  # Runtime stage
  - ca-certificates
  - crypto-policies-scripts
arches:
  - x86_64
  - aarch64
context:
  bare: true
